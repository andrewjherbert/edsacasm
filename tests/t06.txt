; Test 6 -- Arithmetic check (-1) + (+1) = 0 -- should finish with 0 in Acc
.Test06.	T .DUMMY. D		; clear Acc
			A .MONE. D      ; load -1
			A .PONE. D      ; add +1
			Z F				; halt

; data align on even word boundary
@
.DUMMY.		+0L				; temporary store
.PONE.		+1L
.MONE.      -1L
